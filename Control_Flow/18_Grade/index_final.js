const marks = [90, 90, 75];

console.log(calculateGrade(marks));

function calculateGrade(marks) {

    if (average < 60) return "F";
    if (average < 70) return "D";
    if (average < 80) return "C";
    if (average < 90) return "B";
    return "A";

    eng
}


function calculateAverage(array) {
    let sum = 0;
    for (let value of array)
        sum += value;
    let average = sum / average.length;
    return average;
}


console.log(gradeCalculate(points));



const points = [45, 100, 100, 75];

function gradeCalculate() {
    if (average < 60) return "F";
    if (average < 70) return "D";
    if (average < 80) return "C";
    if (average < 90) return "B";
    return "A";
}

function gradeAverage(array) {
    let sum = 0;
    for (let value in array) {
        sum += value;
    }
    let average = sum / array.length;
    return average;
}



// use function and function parameter whih is the array being used 
// in the output