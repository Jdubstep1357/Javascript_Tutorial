// takes 2 parameters, width, he
// ight and returns true if 
// width > height, otherwise false

let landscape = isLandscape(5, 4)
console.log(landscape);

function isLandscape(width, height) {
    return (width > height);
}