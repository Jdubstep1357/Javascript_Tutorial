// If a customer has more than 100 points, are 'gold'
// Otherwise are 'silver' customer


let points = 110
// if points are above 100, print out gold. otherwise get silver
let type = points > 100 ? 'gold' : 'silver';

console.log(type);